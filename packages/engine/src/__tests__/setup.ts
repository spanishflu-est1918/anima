// Vitest setup file for engine tests

import { afterEach, beforeEach } from 'vitest'

// Clear console errors/warnings between tests if needed
beforeEach(() => {
  // Setup before each test
})

afterEach(() => {
  // Cleanup after each test
})
