{
  "name": "@anima/engine",
  "version": "0.1.0",
  "description": "UI-agnostic game engine for Anima adventures",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome check ."
  },
  "dependencies": {
    "@anima/storyscript": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^20.11.0",
    "vitest": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
