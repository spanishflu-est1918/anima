{
  "name": "anima",
  "version": "0.1.0",
  "private": true,
  "description": "Point-and-click adventure game engine",
  "scripts": {
    "dev": "concurrently \"pnpm --filter game dev\" \"pnpm --filter api dev\"",
    "build": "pnpm --filter game build && pnpm --filter api build",
    "lint": "pnpm --filter game lint && pnpm --filter api lint",
    "typecheck": "pnpm --filter game typecheck && pnpm --filter api typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
